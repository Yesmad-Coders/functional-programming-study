// 코드 1-3 값으로서의 함수

// 변수에 100을 담고 있다.
var vOne = 100;

// 변수에 함수를 담고 있다.
var vTwo = function () {};

// 100을 리턴한다.
function fOne() {
  return 100;
}

// 함수를 리턴한다.
function fTwo() {
  return function () {};
}
